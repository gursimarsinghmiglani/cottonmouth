// every function has save pointer in beginning
// returns ast pointer

#pragma once
#include "ast.hpp"
global int input_ptr = 0;
inline AST *program(std::vector<Lexeme> lexemes) {
    AST ast
    while (input_ptr < lexemes.size()) {

